<% if current_user %>
  <% if post.favorited_by?(current_user) %>
    <%= button_to post_favorites_path(post), method: :delete, class: "btn" do %>
      <span class="favorite-count liked">❤️ <%= post.favorites.count %></span>
    <% end %>
  <% else %>
    <%= button_to post_favorites_path(post), method: :post, class: "btn" do %>
      <span class="favorite-count unliked">🤍 <%= post.favorites.count %></span>
    <% end %>
  <% end %>
<% end %>
