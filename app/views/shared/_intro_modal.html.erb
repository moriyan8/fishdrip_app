<div class="modal fade" id="introModal" tabindex="-1" aria-labelledby="introModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-centered">
    <div class="modal-content bg-dark text-light">
      <div class="modal-header">
        <h5 class="modal-title" id="introModalLabel">ようこそ FishDrip へ！</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="閉じる"></button>
      </div>
      <div class="modal-body">

        <!-- Carousel -->
        <div id="introCarousel" class="carousel slide" data-bs-interval="false">
          <div class="carousel-inner">

            <!-- スライド1 -->
            <div class="carousel-item active text-center">
              <img src="<%= asset_path('home.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="投稿画面の説明" style="max-height: 300px;">
              <p>釣果を投稿して仲間とシェアできます 🎣</p>
            </div>

            <!-- スライド2 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:show.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="マップ機能の説明" style="max-height: 300px;">
              <p>地図で釣果を確認！次の釣行に活かせます 🗺️</p>
            </div>

            <!-- スライド3 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:new1.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>NFT を集めてコレクション！プロフィール設定も可能です ✨</p>
            </div>

            <!-- スライド4 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:new2.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>NFT を集めてコレクション！プロフィール設定も可能です ✨</p>
            </div>

            <!-- スライド5 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:map.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>NFT を集めてコレクション！プロフィール設定も可能です ✨</p>
            </div>

            <!-- スライド6 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('users:show.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>NFT を集めてコレクション！プロフィール設定も可能です ✨</p>
            </div>

            <!-- スライド7 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('nft.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>NFT を集めてコレクション！プロフィール設定も可能です ✨</p>
            </div>

          </div>

          <!-- ナビゲーション -->
          <button class="carousel-control-prev" type="button" data-bs-target="#introCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#introCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
          </button>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">閉じる</button>
      </div>
    </div>
  </div>
</div>

<script>
  (function() {
    function showIntroModalIfExists() {
      const el = document.getElementById('introModal');
      if (!el) return;

      // bootstrap がロードされているか確認
      const Modal = window.bootstrap && window.bootstrap.Modal;
      if (!Modal) {
        console.warn("Bootstrap JS が存在しません。cdn で bootstrap.bundle.min.js を読み込むか、application.js に import してください。");
        return;
      }

      const modal = new Modal(el);
      modal.show();
    }

    // Turbo を使っている場合
    document.addEventListener('turbo:load', showIntroModalIfExists);
    // 通常ロード
    document.addEventListener('DOMContentLoaded', showIntroModalIfExists);
  })();
</script>
