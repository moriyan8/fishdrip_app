<div class="modal fade" id="introModal" tabindex="-1" aria-labelledby="introModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-centered">
    <div class="modal-content bg-dark text-light">
      <div class="modal-header">
        <h5 class="modal-title" id="introModalLabel">ようこそ FishDrip へ！</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="閉じる"></button>
      </div>
      <div class="modal-body">

        <!-- Carousel -->
        <div id="introCarousel" class="carousel slide" data-bs-interval="false">
          <div class="carousel-inner">

            <!-- スライド1 -->
            <div class="carousel-item active text-center">
              <img src="<%= asset_path('home.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="Home画面の説明" style="max-height: 300px;">
              <p>Home画面ではみんなが投稿した釣果をみることができます！</p>
            </div>

            <!-- スライド2 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:show.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="投稿詳細画面の説明" style="max-height: 300px;">
              <p>投稿をクリックすると投稿内容の詳細が確認できます！どこで釣れたのか？など詳しい内容が確認できる！</p>
            </div>

            <!-- スライド3 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:new1.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="投稿機能の説明" style="max-height: 300px;">
              <p>自分で釣果を投稿してみよう！魚の名前や釣れた場所、魚のサイズなどを共有・記録できます🐟</p>
            </div>

            <!-- スライド4 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:new2.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="投稿機能（Map）の説明" style="max-height: 300px;">
              <p>釣れた場所はMapで記録投稿できる！釣れた場所をMap上で選択しよう！釣れた場所を共有したくない場合は最後に記録をタッチ！自分だけの記録にしよう！</p>
            </div>

            <!-- スライド5 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('posts:map.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="Map機能の説明" style="max-height: 300px;">
              <p>Mapでは自分が投稿記録した場所を一覧で確認できる！青いピンは投稿でオレンジピンは記録した場所✨ピンをクリックすると簡単な詳細も確認可能！</p>
            </div>

            <!-- スライド6 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('users:show.jpg') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="マイページの説明" style="max-height: 300px;">
              <p>自分が投稿・記録したものを確認できる！また投稿するとNFTミントボタンが出てきます！ボタンをクリックすると→</p>
            </div>

            <!-- スライド7 -->
            <div class="carousel-item text-center">
              <img src="<%= asset_path('nft.png') %>" class="d-block mx-auto img-fluid rounded mb-3" alt="NFT機能の説明" style="max-height: 300px;">
              <p>ここで自分のウォレットを接続するとNFTがもらえる！</p>
            </div>

          </div>

          <!-- ナビゲーション -->
          <button class="carousel-control-prev" type="button" data-bs-target="#introCarousel" data-bs-slide="prev">
            <span class="carousel-control-prev-icon"></span>
          </button>
          <button class="carousel-control-next" type="button" data-bs-target="#introCarousel" data-bs-slide="next">
            <span class="carousel-control-next-icon"></span>
          </button>
        </div>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">閉じる</button>
      </div>
    </div>
  </div>
</div>

<script>
  (function() {
    function showIntroModalIfExists() {
      const el = document.getElementById('introModal');
      if (!el) return;

      // bootstrap がロードされているか確認
      const Modal = window.bootstrap && window.bootstrap.Modal;
      if (!Modal) {
        console.warn("Bootstrap JS が存在しません。cdn で bootstrap.bundle.min.js を読み込むか、application.js に import してください。");
        return;
      }

      const modal = new Modal(el);
      modal.show();
    }

    // Turbo を使っている場合
    document.addEventListener('turbo:load', showIntroModalIfExists);
    // 通常ロード
    document.addEventListener('DOMContentLoaded', showIntroModalIfExists);
  })();
</script>
